import React, { Component } from 'react';

class Orders extends Component {
  static propTypes = {
  };

  render() {
    return (
      <ul>
        <h1>ORDERS</h1>
      </ul>
    );
  }
}

export default Orders;
